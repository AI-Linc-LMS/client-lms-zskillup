// Promoted to components/billing/LockedRow.tsx (the shared Phase 8 visible-lock
// primitive). Re-exported here so existing company-hub imports keep working.
export { LockedRow } from '@/components/billing/LockedRow';
